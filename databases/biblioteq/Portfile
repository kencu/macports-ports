# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0
PortGroup           qmake5 1.0

github.setup        textbrowser biblioteq 2023.07.20
revision            0
github.tarball_from archive
distname            BiblioteQ-${version}

categories          databases
license             BSD-3-Clause
maintainers         {i0ntempest @i0ntempest} openmaintainer

description         GUI editor for SQLite databases
long_description    ${description}

homepage            https://textbrowser.github.io/biblioteq/

checksums           rmd160  f89e6b233b44619dd366d43783be3d265bed2bc5 \
                    sha256  4917ec8898312221fd38c8f917e5b0f9c4e07daba2a9e5fb0f7a51973bd5e984 \
                    size    73547221

patchfiles          patch-biblioteq-macports.diff patch2.diff

#universal_variant   no

compiler.cxx_standard 2011

configure.args-append biblioteq.osx.pro

# we have to clear this for the build's post-install scripts to work
#destroot.post_args-delete ${destroot.destdir}
#destroot.post_args
destroot.env        "DESTROOT="

#pre-configure {
#   reinplace "s|BiblioteQ.d|${destroot}${worksrcpath}/BiblioteQ.d|g" ${worksrcpath}/biblioteq.osx.pro
#}

post-destroot {
        copy "${worksrcpath}/BiblioteQ.d/BiblioteQ.app" ${destroot}${applications_dir}
}



platform darwin {

#    configure.args-append \
#                        CONFIG+=c++11 \
#                        INCLUDEPATH+=${qt_includes_dir} \
#                        QMAKE_LIBDIR+=${qt_libs_dir}

    # Workaround xcrun: error: SDK "macosx12" cannot be located
#    configure.sdk_version
    # Uses QSysInfo::buildAbi and QSysInfo::currentCpuArchitecture
#    qt5.min_version     5.4

#    qt5.depends_component \
#                        qtmacextras
#    qt5.depends_build_component \
#                        qttools

#    destroot {
#            copy "${worksrcpath}/src/DB Browser for SQLite.app" ${destroot}${applications_dir}
#    }

#    post-destroot {
#            ln -s "${applications_dir}/DB Browser for SQLite.app/Contents/MacOS/DB Browser for SQLite" ${destroot}${prefix}/bin/sqlitebrowser
#    }
}
